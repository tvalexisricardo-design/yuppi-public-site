/**
 * Muda para `true` quando tiveres profissionais suficientes para lançar a
 * página /profissionais publicamente (menu + sitemap). As páginas de perfil
 * continuam a funcionar via link direto mesmo com isto a `false` — isto só
 * controla a visibilidade na navegação e na descoberta pelo Google.
 */
export const SHOW_PROFISSIONAIS = false;
